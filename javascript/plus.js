//fub para somar dois núemros

const numero1 = Number(prompt ("digitar um número:"))
const numero2 = Number(prompt("digite outro número:"))

const resultado = (numero1 + numero2)

alert("Resultado de " + numero1 + " + " + numero2 + " = " + resultado )